import { Grid, Typography } from "@mui/material";

const TopDoctors = () => {
  return (
    <Grid>
      <Typography>Top Doctors to Book</Typography>
      <Typography>List of top doctors will be displayed here.</Typography>
    </Grid>
  );
};

export default TopDoctors;
